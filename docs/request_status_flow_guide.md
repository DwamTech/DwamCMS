# 🔍 منطق ورحلة الاستعلام عن حالة الطلب - Frontend Logic Guide

هذا المستند يشرح المنطق الكامل لخاصية "الاستعلام عن حالة الطلب" (Track Request Status)، بدءاً من دخول المستخدم للصفحة وحتى عرض النتيجة، لتسهيل التنفيذ على فريق الـ Frontend.

---

## 1️⃣ رحلة المستخدم (The User Journey)

### الخطوة 1: الدخول لصفحة الاستعلام
- يدخل المستخدم إلى صفحة "الاستعلام عن الطلب" (Track Request).
- تظهر له واجهة بسيطة وواضحة تطلب منه بيانات التحقق.

### الخطوة 2: إدخال البيانات
يجب أن يطلب النظام من المستخدم معلومتين أساسيتين لضمان الخصوصية (لن يتم عرض حالة الطلب بمجرد رقم الطلب فقط):
1.  **رقم الطلب (Request Number):** الرقم الذي حصل عليه عند تقديم الطلب (مثل: `0045` أو `IND-2025-0045`).
2.  **رقم الجوال (Phone Number):** الرقم الذي استخدمه في نموذج التقديم.

### الخطوة 3: اختيار نوع الطلب (اختياري/أو تلقائي)
يمكن تصميم الواجهة بطريقتين:
*   **TABs (تبويبات):** تبويب لـ "الأفراد" وتبويب لـ "المؤسسات".
*   **Dropdown:** قائمة لاختيار نوع الطلب.

### الخطوة 4: إرسال الطلب (Action)
- يضغط المستخدم على زر "بحث" أو "استعلام".
- تظهر حالة تحميل (Loading Spinner) لمنع التكرار ولإشعار المستخدم بأن العملية جارية.

---

## 2️⃣ التنفيذ التقني (Technical Implementation)

يعتمد الرابط (Endpoint) على نوع الطلب الذي اختاره المستخدم.

### 🅰️ خيار 1: استعلام الأفراد (Individual)
**Endpoint:**
```http
POST /api/support/individual/status
Content-Type: application/json
```

### 🅱️ خيار 2: استعلام المؤسسات (Institutional)
**Endpoint:**
```http
POST /api/support/institutional/status
Content-Type: application/json
```

### 📦 البيانات المرسلة (Payload)
هي نفسها للحالتين:
```json
{
    "request_number": "0045",    // string - مطلوب
    "phone_number": "0501234567" // string - مطلوب
}
```

---

## 3️⃣ معالجة الردود والتفاعل (Response & Interactive Logic)

يجب أن تتحول الصفحة إلى واجهة تفاعلية بناءً على الـ `status` الذي يعود في الـ Response.

```json
// Example Success Response
{
    "status": "waiting_for_attachments", // الحالة هي المحرك الرئيسي للصفحة
    "rejection_reason": "يرجى إعادة إرفاق الهوية، الصورة غير واضحة", // قد يحتوي على ملاحظات المراجعة
    "created_at": "2025-01-07"
}
```

فيما يلي السيناريوهات التفصيلية لكل حالة:

### 🟡 الحالة 1: قيد المراجعة (pending)
**اللون المقترح:** أصفر/برتقالي.
**الإجراء:** عرض بطاقة معلومات فقط.
*   **العنوان:** الطلب قيد المراجعة.
*   **الوصف:** "طلبك حالياً قيد المراجعة من قبل الإدارة المختصة."
*   **لا يوجد إجراء مطلوب من المستخدم.**

---

### 🟢 الحالة 2: مقبول (approved)
**اللون المقترح:** أخضر فاتح.
**الإجراء:** عرض رسالة قبول مبدئي.
*   **الوصف:** "تمت الموافقة المبدئية على طلبك."

---

### 🎉 الحالة 3: مكتمل (completed)
**اللون المقترح:** أخضر داكن / مميز.
**الإجراء:** عرض رسالة الاكتمال أو التهنئة.
*   **العنوان:** تم إكمال الطلب بنجاح.
*   **الرسالة:** يجب عرض محتوى الحقل `admin_response_message` القادم من الباك إند (إذا وُجد) كرسالة مخصصة للمستخدم.
    *   *مثال:* "تم تحويل مبلغ الدعم إلى حسابكم البنكي."
*   **وإلا:** عرض رسالة افتراضية: "تم استكمال جميع إجراءات طلبكم بنجاح."

---

### 🔴 الحالة 4: مرفوض (rejected)
**اللون المقترح:** أحمر.
**الإجراء:** عرض سبب الرفض بوضوح لضمان الشفافية.
*   **العنوان:** تم رفض الطلب.
*   **الوصف:** "نعتذر، لم يتم قبول طلبك."
*   **سبب الرفض:** يجب عرض محتوى الحقل `rejection_reason` القادم من الباك إند في مربع واضح.
    *   *مثال:* "السبب: عدم استيفاء شروط الدخل المحددة."

---

### ⚠️ الحالة 5: بانتظار مرفقات (waiting_for_attachments) - 🔥 حالة تفاعلية
هذه هي الحالة الأخطر والأهم. تعني أن المراجع وجد نقصاً في الملفات أو أن الملفات غير واضحة.

**اللون المقترح:** أزرق أو برتقالي داكن.
**الإجراء:** تحويل الصفحة إلى **نموذج رفع ملفات (Upload Form)**.

**تفاصيل التفاعل المطلوب:**
1.  عرض رسالة تنبيه: *"يوجد ملاحظات على المرفقات الخاصة بطلبك. يرجى مراجعة الملاحظة أدناه وإعادة رفع الملفات المطلوبة."*
2.  عرض الملاحظات: عرض محتوى `rejection_reason` (في هذه الحالة يستخدم لكتابة الملاحظات للمستخدم).
    *   *مثال للملاحظة:* "صورة الهوية غير واضحة + ملف دراسة الجدوى ناقص".
3.  **اظهار حقول رفع الملفات:** (كما هو موضح سابقاً).

---

### ❌ الحالة 6: طلب غير موجود (404 Not Found)
**الإجراء:**
عرض رسالة خطأ واضحة: *"عذراً، لم نعثر على أي طلب مطابق لرقم الطلب ورقم الهاتف المدخلين."*

---

## 4️⃣ ملخص الـ Flow Chart المقترح (Logic Flow)

---

## 4️⃣ ملخص الـ Flow Chart المقترح (Logic Flow)

1.  **Start:** المستخدم يفتح الصفحة.
2.  **Input:** يدخل (رقم الطلب + الجوال).
3.  **Click:** يضغط "بحث".
4.  **Loading:** تفعيل وضع التحميل + تعطيل زر البحث.
5.  **API Call:** إرسال Request للـ Backend.
6.  **Check Status Code:**
    *   **If 200:** ✅ اعرض تفاصيل الحالة والرسالة.
    *   **If 404:** ❌ اعرض "بيانات غير صحيحة".
    *   **If 422:** ⚠️ اعرض "أخطاء الإدخال".
    *   **Otherwise:** 💥 اعرض "حدث خطأ غير متوقع، حاول لاحقاً".
7.  **Finish:** إعادة تفعيل زر البحث لمحاولة جديدة.

---

## 💡 نصائح لتحسين التجربة (UX Tips)

*   **تذكرني (Remember Me):** (اختياري) يمكن حفظ رقم الجوال في الـ LocalStorage لتسهيل الاستعلام المستقبلي.
*   **تنسيق رقم الطلب:** إذا كان رقم الطلب مجرد أرقام، اطلب `numeric keyboard` على الجوال.
*   **الوضوح:** إذا كانت الحالة "مرفوض"، وبرمجنا الباك إند لإرسال سبب الرفض في الـ `message`، تأكد من عرضه بوضوح للمستخدم ليعرف السبب.
